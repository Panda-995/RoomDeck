# Isolated automated tests ONLY. Never use these credentials for deployment.
port: 7880
bind_addresses: [127.0.0.1]
rtc:
  node_ip: 127.0.0.1
  use_external_ip: false
  tcp_port: 7881
  udp_port: 7882
  use_ice_lite: true
keys:
  roomdeck-test: test-only-secret-for-isolated-local-tests-2026
