# RoomDeck 界面设计规范

2026-09-10 更新，适用于 `web/src` 的实际产品。风格为轻简、温和、有少量修饰：暖白背景、深绿色操作、淡米色便签与浅灰蓝链接。公共大屏保留深色观看环境。早期 `prototype` 和开发设计文档中的琥珀色方案属于历史原型，后续界面开发以本文件和生产代码为准。

## 页面与层级
主持人：房间身份和状态 → 内容 / 投屏 / 游戏 → 当前区域操作。照片、文件、便签、链接、投票作为内容的第二级筛选，避免与游戏、投屏混排。宽屏右侧保留现场大屏和房间管理；中小屏管理快捷入口位于标题下方，现场控制可直接定位到控制区域。

参与者：手机底部四个入口为内容、投屏、分享、游戏，顶部不再重复相同导航；桌面显示页面导航，取消固定底栏。内容底部留出安全区，最后一项可以完整滚动到工具栏上方。

公共大屏：大图 → 活动身份 → 加入二维码。投屏占用独立观看区域，保持远距离可读。

## 组件
| 项目 | 规范 |
| --- | --- |
| 页面背景 | `#f7f8f5`，边缘淡绿 `#edf1e7` 与暖米 `#f3efe5` 纸张叠层 |
| 正文 / 次级正文 | `#263b33` / `#5d6b64` |
| 主操作 / 柔和选中背景 | `#30664d` / `#e8f0e8` |
| 分隔线 | `#dde4dc` |
| 正文 | 15px，行高 1.65；辅助文字 12–14px |
| 按钮与图标操作 | 最小 44px，文字与图标居中；禁用状态不可重复提交 |
| 输入控件 | 最小 45px；手机输入文字 16px，避免聚焦时放大 |
| 卡片 | 14–16px 圆角、单层边框；同排内容卡片页脚对齐 |
| 弹窗 | 桌面 560px，详情宽版 900px；手机底部面板，上限 92dvh |
| 间距 | 以 8px 为主要步长，4px 用于细节调整 |

内容卡片保留复制、下载和精选等常用操作；隐藏和删除收进带文字的“更多操作”。照片网格裁切，详情完整显示。

弹窗采用原生 dialog：标题可访问、焦点限制在弹窗内、关闭后恢复到触发按钮。标题与关闭按钮固定在滚动区域顶部。提示信息显示于当前弹窗内，避免被浏览器顶层遮挡。点击背景不丢弃表单；普通状态支持 Escape，上传或保存过程中禁止误关闭。

分享默认选中首个可用类型，上传区支持点选和拖放；失败可单项重试，重试期间锁定类型。游戏猜点与秘密投票先选再确认，同步刷新不清除未提交的选择；轮次/阶段改变时清空选择。卧底词在离开游戏页面、窗口失焦、页面隐藏时收起。

## 响应式与维护

- 1280px 起采用侧栏、内容、大屏控制三栏；900–1279px 控制区域移到内容下方；900px 以下隐藏侧栏。
- 640px 以下切换手机导航与弹窗，英文较长的导航名称允许换行。
- 样式加载顺序固定为 `style.css` → `layout.css` → `activities.css` → `motion.css` → `background.css` → `interactions.css` → `polish.css`。基础组件和对应断点放在一起，避免在后加载的文件重复定义通用导航。
- 中英文共用布局和图标，选中状态有 `aria-pressed`，开关保留标签与键盘操作。
- 尊重 `prefers-reduced-motion`，所有状态同时有文字表达。

## 交付与检查
背景修饰仅在视口右上与左下边缘出现，手机缩小露出面积。独立固定底层不参与布局，设置 `pointer-events: none` 与 `aria-hidden`；卡片和弹窗保持实色底。背景不使用图片、模糊或持续动画，公共大屏不加载此装饰，强制色彩模式隐藏装饰。

新版实机截图见 `docs/screenshots/ui-after`；检查记录见 `docs/UI-UX-AUDIT.md`。预览素材使用隔离的测试房间与示例照片，不代表用户内容。自动无障碍扫描只能覆盖可检测规则，不能替代真实手机、屏幕阅读器和电视设备验收。

动效规格见 `docs/MOTION.md`，桌面与手机操作录屏见 `docs/ui-motion.html`。主要界面动作在 140–260ms 内完成；导航位置固定，选中底色连续移动；退出比进入快；游戏结果只有一次短反馈。键盘与减少动态效果模式即时响应。

## 2026-09-11 大屏与交互组件

大屏使用深绿舞台、边缘柔光和低对比圆弧，内容面板保持实色；背景不接收指针事件。扫码区使用独立纸色卡片。欢迎、照片、精选和投票沿用相同排版；720p 与 1080p 照片和投票模式均检查内容高度，照片作者与序号保留独立空间。超长内容允许在内容区滚动。

所有现有单选下拉框接入 `pretty-select` 指令，保留原生表单值、双语标签和焦点，使用 Popover 顶层渲染选项，防止被弹窗或滚动容器裁切。支持方向键、Home/End、Enter、Escape、Tab 和文字查找；鼠标点选后恢复焦点，外部点击、滚动及窗口变化关闭菜单。菜单使用 150ms 入场，尊重减少动态效果偏好。不支持 Popover 的旧浏览器保留原生选择框。

按钮与输入框统一 11px 圆角；下拉菜单 14px 圆角、淡绿选中态和几何勾选标记。紧凑轮播间隔保留固定宽度，避免挤压左侧标签。预览见 `docs/display-ui-preview.html`，截图来自隔离测试房间。

## 2026-09-12 画板与协同主持

游戏页第二级分为「聚会游戏」和「画板 / 你画我猜」，保持原导航。绘画工具仅向当前可作画角色展示；猜题者保留画面、答案输入和积分。画布以 1000×625 逻辑坐标映射鼠标/触摸输入；大屏为画布和积分分配独立空间。

协同主持沿用参与者身份，通过顶部职责提示进入对应功能。原主持人在成员详情中逐项授权，使用有标签的原生复选框，异步操作时禁用重复提交。清空画板、开始新游戏和结束游戏需要在界面内二次确认。新增样式 `board.css` 在 `polish.css` 后加载。
